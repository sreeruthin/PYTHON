def factorial(n):
    fact = 1  # Initialize fact to 1
    i = 1
    while i <= n:
        fact = fact * i
        i += 1
    return fact

n = int(input("Enter the number: "))
result = factorial(n)
print("Factorial of {} is {}".format(n, result))
